# Pi Extension Settings

TypeBox-first configuration runtime and artifact tooling for Pi extensions.

## Install

This package is private. Clone it or install it from Git:

```sh
npm install git+https://github.com/zigai/pi-extension-settings.git
```

## Development

```sh
npm install
just check
just coverage
```
